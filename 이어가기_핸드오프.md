# 이어가기 핸드오프 — 주식리포트 3.0

> 2026-08-02 · 이 세션이 느려져 폐기. 새 세션은 이 문서부터 읽으면 됨.

---

## 1. 지금 어디까지 왔나

| 단계 | 상태 |
|---|---|
| 화면 설계 확정 | ✅ 완료 |
| 지침서 작성 | ✅ 완료 (`주식리포트_3.0_화면_지침서.md`) |
| **1단계 — `morning_call.py`** | ✅ **완료 · 푸시됨** (`479f7c0`, stock-report 저장소) |
| 2단계 — `render_html.py` 골격 교체 | ⬜ 미착수 ← **다음 할 일** |
| 3~8단계 | ⬜ 미착수 |

---

## 2. 파일이 어디 있나 (중요 — 저장소가 둘이다)

| 무엇 | 어디 |
|---|---|
| **지침서 · 시안** | `cafe-landing` 저장소, 브랜치 `claude/stock-report-dev-transition-h4desb` |
| **실제 코드** | `stock-report` 저장소 (사장님 PC의 `stock_report/`) |

지난번 로컬 클로드코드가 지침서를 못 찾아 멈춘 원인이 이것이다.
**새 세션 시작 전에 지침서를 `stock_report/01_문서_설계명세서/`로 복사해 둘 것.**

이 저장소(`cafe-landing`)에 있는 파일:
- `주식리포트_3.0_화면_지침서.md` — 구현 지침 (483줄)
- `주식리포트_3.0_시안.html` — 화면 시안
- `이어가기_핸드오프.md` — 이 문서

---

## 3. 1단계에서 밝혀진 것

### ⚠️ §② 일본 수식 오류 — **지침서는 아직 틀린 상태다**

지침서 §② 초판의 `sign(-jpy_chg)`가 **틀렸다.** `jpy_chg`는 `JPY=X` 변화율이고
**음수 = 엔 절상**(`generate_report.py:663` — `if jpy <= -1.0:  # JPY=X 하락 = 엔 절상`
→ 위험 가산). 엔 절상은 일본 수출주에 부담이므로 **음의 기여**여야 하고,
그러려면 부호를 뒤집지 않은 **`sign(jpy_chg)`**가 맞다.
같은 문단의 괄호 설명이 맞고 수식이 틀렸던 것이다.

- **로컬 1단계 구현은 이미 올바른 `sign(jpy_chg)`로 들어갔다** (사장님 확인 완료, `479f7c0`).
- **지침서 본문은 사장님 지시로 되돌려 놓았다 — 아직 `sign(-jpy_chg)`로 적혀 있다.**
  다음 세션에서 지침서를 읽을 때 **이 문단이 코드보다 우선하지 않도록 주의할 것.**
  지침서를 고칠지 그대로 둘지는 사장님 결정 사항.

### ✅ 실행 순서 충돌 — 로컬에서 해결됨

`overnight_snapshot.json`은 `generate_report`가 쓰는데 `morning_call`은 그 **전에** 돈다.
로컬 구현이 정본 생성기 `collect_overnight_gap()`을 직접 호출하도록 처리 —
산식을 베끼지 않아 불변조항 2(단일 원천)를 지킨다. **이 처리는 지침서에 아직 안 적혔다.**

---

## 4. 2단계 착수 전 반드시 처리할 것 (지침서 미반영)

### ★ EWY 정체 확인 — §8 열어둔 문제 **해결됨. 단 지침서·시안이 아직 틀린 상태**

`generate_report.py:632-648` 확인 결과:

```python
_e1 = (EWY 종가 / EWY 전일종가 - 1) * 100      # EWY 1일 등락률
_k1 = (코스피 종가 / 코스피 전일종가 - 1) * 100  # 코스피 1일 등락률
ewy_sig = round(_e1 - _k1, 2)                   # ← 이게 ewy_gap
```

**`ewy_gap`은 갭이 아니라 "EWY 1일 등락률 − 코스피 1일 등락률"(%p 차이)이다.**
뜻은 *"미국 투자자들이 어제 한국 시장의 움직임을 따라왔는가"*.

**그래서 지금 시안의 근거 문장은 틀렸다:**

> ❌ "미국 상장 한국 ETF(EWY)가 **−20.5% 갭**으로 마감"
> → EWY가 20.5% 폭락한 것처럼 읽힌다. 아니다. **차이가** 20.5%p인 것이다.

**2단계에서 고칠 것:**

1. 근거 문장은 **두 다리를 다 보여줄 것.** 스프레드만 쓰면 오독된다.
   문형 예: *"어제 코스피는 X%였는데 미국 상장 한국 ETF(EWY)는 같은 날 Y%였어요 —
   미국 쪽이 그 움직임을 따라오지 않았다는 뜻입니다(차이 Z%p)."*
2. **문제:** `overnight_snapshot.json`은 스프레드(`ewy_gap`)만 저장하고 두 다리를 안 남긴다.
   화면이 역산하면 불변조항 2 위반. → `collect_overnight_gap()`이 `ewy_1d`·`kospi_1d`를
   스냅샷에 **함께 저장**하도록 상류를 먼저 고칠 것 (작은 변경, 2단계 앞에 붙이면 됨).
3. §① 표의 `ewy_gap` 설명도 "갭" → "코스피 대비 차이(%p)"로 고칠 것.

### ★ 보정 범위 밖 값 주의

`generate_report.py:666`의 임계값은 `-2.5` / `-3.5`인데 오늘 값은 **−20.46**.
**보정 분포를 한참 벗어난 값이다.** 판정은 `sign()`만 쓰므로 1단계는 안전하지만,
근거 문장에서 이 값을 강한 신호처럼 단정하지 말 것. §8에 남은 과제.

### ★ 주말 행 채점 (로컬 클로드코드 지적)

파이프라인이 주말에도 돌아 **채점 불가능한 예측 행이 주당 2건씩 쌓인다.**
현재 `score_pending`은 손대지 않고 그냥 둔다(거짓말 안 함 — 올바른 처리).
**§⑦에서 "누적 N일"과 적중률을 셀 때 반드시 `hit IS NOT NULL` 기준으로 셀 것.**
→ 6단계에서 챙기기로 로컬과 합의됨.

---

## 5. 다음 세션에 줄 지시 (그대로 붙여넣기)

```
stock_report/01_문서_설계명세서/주식리포트_3.0_화면_지침서.md 와
이어가기_핸드오프.md 를 읽어라.

핸드오프 §4를 먼저 처리한다:
1. collect_overnight_gap()이 ewy_1d·kospi_1d를 스냅샷에 함께 저장하도록 상류 수정
2. 그 다음 지침서 §6의 2~3단계 — render_html.py 골격 교체(시간축 7블록) +
   ①밤사이 ②아침예측 ③위험해석 구현
3. 근거 문장은 핸드오프 §4의 문형을 따를 것 — ewy_gap 스프레드만 쓰지 마라

§4 이후(④~⑦)는 아직 손대지 마라. 끝나면 멈추고 보고해라.
```

---

## 6. 확정된 설계 (변경 금지 — 이미 합의됨)

- **화면 7블록**: 밤사이 → 아침예측 → 위험 → 행동1(보유) → 행동2(BNF) → 사건 → 성적
- **행동 문구 15개**: `config.FINAL_ACTION_KO` **값만** 교체 (키 추가·삭제 금지 —
  `07_회귀테스트/test_net.py:5751` 키 일치 검사가 깨진다)
- **띠**: ▼줄이기 3 · ■지키기 6 · ▲사기 4 · ◇미보유 2 — 표시 계층 전용, 판단 로직 무관
- **적중률 70%는 화면에 쓰지 않는다.** `morning_call_daily` 20거래일 실측 후 별도 작업.
