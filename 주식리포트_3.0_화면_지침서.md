# 주식리포트 3.0 화면 지침서

> 확정일 2026-08-02 · 대상 `render_html.py`(주), `render_md.py`·`render_xlsx.py`(종속)
> 근거 시안: `3.0_design_v2.html` (아침 관제탑)

---

## 0. 왜 다시 만드나

지금 리포트는 **숫자는 많은데 아침에 쓸모가 없다.** 위험지수 59.8을 봐도 오늘 한국장이
어떻게 열릴지 알 수 없고, 뉴스는 보유 종목과 무관한 것까지 섞여 있다.

3.0의 목적은 하나다 — **아침 8시에 이 화면 하나만 보고 오늘 시장을 이해하고 행동을 정한다.**

그래서 화면을 **시간 순서**로 재배치한다. 밤사이 무슨 일이 있었고 → 그래서 오늘 아침
한국·일본은 어떻게 열릴 것 같고 → 위험은 이 숫자이며 그 뜻은 이렇고 → 그래서 오늘 할 행동은
이것이다. 이 흐름을 위에서 아래로 읽으면 하나의 이야기가 되어야 한다.

### 핵심 발견 — 재료는 이미 있었다

`overnight_snapshot.json`은 매일 아침 아래를 수집하는데 **화면에 거의 쓰이지 않고 있다.**

| 필드 | 오늘값 | 의미 |
|---|---|---|
| `ewy_gap` | −20.46 | 미국 상장 한국 ETF 갭 — **한국 개장 방향 선행 신호** |
| `nkd_chg` | −0.68 | 닛케이 선물 — **일본 개장 방향 선행 신호** |
| `jpy_chg` | −3.61 | 엔화 3일 — 엔 급등은 일본 수출주 부담 |
| `nq_chg` | +0.59 | 나스닥 선물 — 미국 분위기 |
| `gap_bonus` | 6.0 | 위 신호가 종합위험에 더해진 가산점 |

**이 지침서의 가장 중요한 변경은 이 재료를 화면 맨 위로 끌어올려 "오늘 아침 예측"으로
명시적으로 선언하고, 다음날 채점하는 것이다.**

---

## 1. 불변 조항 (어겨서는 안 되는 것)

1. **표시 전용.** 화면은 판단을 보여주기만 한다. 자동 매매 없음, 실행은 항상 사용자 수동.
2. **단일 원천.** 화면은 숫자를 다시 계산하지 않는다. 계산은 상류(`decisions.py`,
   `scoring_*.py`, `jp_bnf_scan.py`)에서 끝나고 화면은 읽어서 배치만 한다.
3. **정직한 결측.** 데이터가 없으면 "없음"이라고 쓴다. 0이나 이전 값으로 메우지 않는다.
4. **없는 성적을 지어내지 않는다.** 검증되지 않은 적중률(예: "70% 확률")을 화면에 쓰지 않는다.
   측정 전까지는 "채점 대기"로 표기한다. → §7 참조
5. **한글 표기.** 사용자에게 한자·일문 원문 노출 금지(`config.KO_GLOSS` 경유).
6. 리포트 생성은 어떤 블록이 실패해도 **중단되지 않는다.** 실패한 블록은 그 사실을 표시한다.

---

## 2. 화면 전체 구조

왼쪽에 시간축 레일을 두고 위에서 아래로 흐른다. 레일 색은 밤(짙은 청회색) → 새벽(호박색)으로 변한다.

```
[머리]  아침 관제탑 · 생성시각 · 자료기준일

밤사이   ① 내가 자는 동안 무슨 일이       ← 미국 마감 + 선물 + 엔화
   │
오늘아침 ② 그래서 오늘 개장은  ★핵심      ← 한국/일본 방향 예측 + 어제 예측 채점
   │
위험도   ③ 숫자와 그 뜻                   ← 59.8 → 주범은 일본 → 나라별 해석
   │
행동 1   ④ 보유 종목 — 달라진 것만        ← 어제와 행동이 바뀐 종목만
   │
행동 2   ⑤ 일본주 BNF 매매 지시           ← 진입/청산/예산
   │
사건     ⑥ 오늘 진짜 중요한 것만          ← 보유·관심 종목 직접 영향만
   │
성적     ⑦ 이 리포트를 믿어도 되나        ← 적중률 실측
```

---

## 3. 블록별 명세

### ① 밤사이 — 내가 자는 동안 무슨 일이

**원천** `overnight_snapshot.json`, 미국 지수 종가(`asof_prices.py` / 스냅샷)

4칸 격자. 각 칸은 **이름 · 값 · 한 줄 뜻**.

| 칸 | 표시 | 뜻 문구 |
|---|---|---|
| S&P500 마감 | 종가 + 등락률 | 자료 기준일 |
| 나스닥 선물 | `nq_chg` % | "미국 분위기 양호/침체" |
| 닛케이 선물 | `nkd_chg` % | "일본 개장 선행" |
| 엔화(3일) | `jpy_chg` % | "엔 급등 = 일본주 부담" (부호에 따라 문구 전환) |

- 상승 `--good` / 하락 `--critical`.
- `available: false`면 4칸 대신 **"밤사이 자료를 받지 못했습니다 — 아래 예측은 생략합니다"**
  한 줄로 대체하고 ②의 예측도 함께 생략한다(추측 금지).

---

### ② 오늘 아침 — 그래서 개장은 ★이 화면의 핵심

**원천** `overnight_snapshot.json` + 국가위험 축(VKOSPI·닛케이 변동성) + 전일 지수 등락

한국·일본 각 1행. 구성은 **국기 · 시장명 · 방향 · 근거 문장**.

#### 방향 판정 규칙 (신규 구현)

새 모듈 `morning_call.py`를 만든다. 판정은 **아래 규칙 그대로** 하드코딩하고, 임의로
바꾸지 않는다(바꾸면 채점 이력이 무효가 된다).

```
한국:  score = sign(ewy_gap) 가중 2  +  sign(nq_chg) 가중 1
일본:  score = sign(nkd_chg) 가중 2  +  sign(-jpy_chg) 가중 1
       (엔 강세 = jpy_chg 음수 = 일본주 부담 → 음의 기여)

score > 0  → "강세 출발 예상"
score < 0  → "약세 출발 예상"
score == 0 → "방향 불명 — 관망"
```

- 결측 필드는 **0 기여**로 두고, 근거 문장에 "일부 자료 없음"을 명시한다.
- 판정에 쓴 값은 전부 근거 문장에 숫자로 드러낸다 — 사용자가 검산할 수 있어야 한다.

#### 근거 문장 규칙

숫자 뒤에 **반드시 "그래서 무슨 뜻"**이 붙는다. 숫자만 두지 않는다.

> 예) 미국 상장 한국 ETF(EWY)가 **−20.5% 갭**으로 마감 — 한국 개장 방향의 가장 빠른 선행
> 신호예요. 공포지수 VKOSPI도 **84**로 역대 상위 1% 극단치입니다. 다만 어제 코스피가
> **+17.9%** 급반등한 직후라 되돌림 성격이 섞여 있어요.

- 반대 방향 재료가 있으면 **"다만 ~"으로 반드시 함께 적는다.** 유리한 근거만 고르지 않는다.

#### 어제 예측 채점 (블록 하단 점선 아래)

> 어제 아침 이 자리에서 **한국 강세 · 일본 약세**로 봤고, 실제로는 코스피 **+17.9%** ·
> 닛케이 **+4.0%**였습니다 — **한국 적중 / 일본 빗나감.**

- 전일 예측이 없으면(첫날) "오늘부터 채점을 시작합니다"로 표기.

#### 저장 — 채점의 근거 (신규 테이블)

`04_DB_분석데이터/report_data.db`

```sql
CREATE TABLE IF NOT EXISTS morning_call_daily (
  date       TEXT NOT NULL,   -- 예측 생성일(실행일)
  market     TEXT NOT NULL,   -- 'KR' | 'JP'
  call       TEXT NOT NULL,   -- 'UP' | 'DOWN' | 'FLAT'
  score      REAL,            -- 판정 점수(위 규칙 결과)
  basis      TEXT,            -- JSON: 판정에 쓴 원시값 전부
  formula    TEXT NOT NULL,   -- 'v1' — 규칙 변경 시 반드시 상향
  actual_pct REAL,            -- 해당일 지수 등락률(사후 채움)
  hit        INTEGER,         -- 1/0 (사후 채움), FLAT은 채점 제외(NULL)
  scored_at  TEXT,
  PRIMARY KEY (date, market)
);
```

- 같은 날 재실행은 **첫 기록 유지**(`INSERT OR IGNORE`) — 오전락·`bnf_dip`와 같은 철학.
- 채점은 다음 실행에서 `actual_pct IS NULL`인 과거 행을 찾아 채운다.
- **`formula`를 올리면 그 이전 이력과 섞어서 적중률을 계산하지 않는다.**

---

### ③ 위험도 — 숫자와 그 뜻

**원천** `risk_history.json`, `bmri.py`, 국가별 축 점수

**상단**: 큰 숫자 `59.8/100` + 분해(`기본 50.8 + 추세경보 3.0 + 개장갭 6.0`) + 상태 알약
+ 최근 12일 스파크라인.

**해석 문장** (점선 아래, 이 블록의 존재 이유):

> 위험이 오른 주범은 **일본**입니다. 한국은 이미 오래 높은 상태라 새 소식이 아니고,
> 미국은 조용해서 오늘 숫자를 끌어올린 건 일본 쪽 변동성이에요.

- **"주범"은 전일 대비 기여 증가분이 가장 큰 나라**로 자동 선정한다.
- 전일 대비 변화가 없으면 "어제와 같은 이유"로 쓴다.

**나라별 3행** — 각 행 구성:

| 요소 | 내용 |
|---|---|
| 점수 | 국가위험 점수 (상한 도달 시 원시값 병기) |
| 위치 | "자기 역사 상위 N%" — 절대값보다 이게 이해에 중요 |
| 왜 | 축별 근거 + 체제 지속일수 ("위기 체제 124일째") |
| 그래서 | `→ 신규 매수 보류` / `→ 평소 모드` 등 행동 한 줄 |

- 오래 높은 상태는 **"오늘 새로 나빠진 게 아니라 계속 높은 상태"**라고 명시한다.
  이게 없으면 매일 같은 경보를 새 경보로 오해한다.

---

### ④ 행동 1 — 보유 종목 (달라진 것만)

**원천** `decisions.py` 산출 + 전일 리포트 판정

**표시 대상: 어제와 행동 등급이 바뀐 종목만.** 나머지는 마지막 줄에 묶는다.

각 행: `종목명 · 국기 · 보유수량 · [행동] · 이유 한 줄`

| 행동 | 색 |
|---|---|
| 그대로 두세요 · 더 사지 마세요 | `--critical` |
| 지켜보세요 | `--warning` |
| 그대로 두세요 | `--good` |

- 이유에 뉴스가 걸리면 `🗞` 표식을 붙이고 ⑥ 사건 블록과 연결한다.
- 마지막 행: **"나머지 21종목 · 특이사항 없음 · 그대로 두세요"** — 전체 개수가 맞아야 한다.
- 바뀐 종목이 하나도 없으면: **"오늘 행동이 바뀐 종목은 없습니다 — 24종목 전부 어제와 같아요."**

---

### ⑤ 행동 2 — 일본주 BNF 매매 지시

**원천** `jp_bnf_scan_daily` · `jp_bnf_scan_runs` · `jp_bnf_orders` (`report_data.db`),
`config.BNF_DIP`, `config.BNF_SIZING`

기존 `render_html._jp_bnf_card`를 이 명세로 재작성한다.

#### 왜 별도 블록인가

BNF는 **이 저장소에서 유일하게 5관문을 통과한 신호**다(일본 860건 +1.828%p, p=0.0196).
보유 종목 판단과 성격이 완전히 다르다 — 보유는 "들고 있는 것을 어떻게 할까"이고,
BNF는 "규칙이 사라고 하면 산다"이다. 섞으면 둘 다 흐려진다.

#### 머리줄

- 알약: `5관문 통과 · 이 저장소 유일`
- 커버리지: `TOPIX500 490종목 전수 · 커버리지 100%`
  - `jp_bnf_scan_runs.coverage < config.BNF_SIZING["min_coverage"]`(0.95)면
    **매수 계획을 만들지 않는다.** 화면에는 `커버리지 미달 — 오늘 매수 계획 없음`을
    경고색으로 표시한다(조용히 넘어가면 검증 범위 밖에서 지시가 나간다).

#### 본문 — 상태별 분기

**(가) 매수 지시가 있을 때**

각 행: `종목명 · 코드 · [매수] · 수량/금액 · 3조건 실측값 · 진입일`

> 소시오넥스트 6526 · **다음 거래일 시가 매수** · 100주 ≈ 98만엔
> 이격도 −23.1 · 5일 −20.1 · RSI 28.4 · 진입 2026-08-04 시가

**(나) 청산 지시가 있을 때** (진입 후 20거래일 도달)

> 다이요유덴 6976 · **오늘 청산** · 100주 · 진입 2026-07-03 → 20거래일 도달

**(다) 오늘 신호 0건일 때 — 빈 화면으로 두지 않는다**

```
오늘 매수 지시 없음 — 490종목 전부 훑었는데 3조건을 모두 채운 종목이 없습니다.
보유 중인 BNF 포지션도 없어 오늘 청산 지시도 없어요.

[아깝게 못 미친 종목 — 내일 다시 볼 것]
소시오넥스트 6526   이격도 −23.1 ✓ · 5일 −20.1 ✓ · RSI 30.9   RSI가 0.9만 더 내려오면 신호
SCREEN홀딩스 7735   이격도 −24.6 ✓ · 5일 −20.5 ✓ · RSI 35.5   RSI 5.5 남음
다이요유덴 6976     이격도 −28.2 ✓ · 5일 −10.4 ✓ · RSI 39.2   RSI 9.2 남음
```

- **근접 후보 선정**: 3조건 중 **2개 이상 충족**한 종목 중, 미충족 조건의 임계값 대비
  거리가 가까운 순 **최대 3개**.
- 충족 조건은 `✓`와 `--good`, 미충족은 회색.
- **이건 매수 지시가 아니다.** "내일 다시 볼 것"이라는 제목으로 성격을 못 박는다.
- 조건을 완화해 신호를 만들어내지 않는다 — 임계값은 `config.BNF_DIP` 하나뿐이고
  화면이 손대지 않는다.

#### 예산 (블록 하단)

```
예산    0 / 500만엔 사용 · 여유 500만엔
[████░░░░░░░░░░░░░░░░]

규칙: 신호 뜨면 다음 거래일 시가에 종목당 100만엔(100주 단위) 매수 → 20거래일 뒤 청산.
같은 종목 재신호는 1회까지 추가(누적 200만엔). 예산을 넘는 신호는 규칙 밖 — 사장님 재량입니다.
```

- 사용액은 **보유 중인 포지션이 묶고 있는 금액**(`_open_by_code` 기준)이다.
  체결 확정되지 않은 계획을 세지 않는다.
- 예산 초과로 재량으로 넘긴 종목이 있으면 **별도 줄로 명시**한다 —
  `예산 초과 2건은 규칙 밖입니다(재량)`. 조용히 빠지면 안 산 주식을 판 것으로 오해한다.

---

### ⑥ 사건 — 오늘 진짜 중요한 것만

**원천** `news_briefing.json`, `official_filings/`, `news_veto.py`

**게재 기준 (이것만):**

1. **보유 종목**에 직접 영향 (규제·소송·리콜·실적 하향·공시)
2. **관심 종목(BNF 편입)**에 직접 영향
3. 위 둘에 해당하는 **공시**

**빼는 것:** 종목과 무관한 거시 뉴스, 지수 등락 소식, 중복 보도, 출처 불명.

각 건: `[태그] · 제목 · 왜 중요한지 2줄 · 날짜 · 출처`

> **[보유 종목 직접 영향]**
> 테슬라 — 美 NHTSA, 서스펜션 결함 의혹 조사 착수
> 보유 16주에 직접 영향을 주는 규제 리스크예요. 위 「행동 1」에 이미 반영돼 있습니다.
> 2026-07-31 · finance.sina.com.cn

- **0건일 때**: "그 외 어제·오늘 수집된 뉴스 중 보유·관심 종목에 영향 있는 건 없었어요.
  **수집은 정상 작동했습니다.**" ← 수집 실패와 구분되어야 한다.
- 수집이 실제로 실패했으면 그 사실을 경고색으로 따로 쓴다.

---

### ⑦ 성적 — 이 리포트를 믿어도 되나

**원천** `04_DB_분석데이터/analyze_db.py`(행동경보 적중률),
`tools/audit/build_indicator_registry.py`(검증 상태), 신규 `morning_call_daily`

3칸:

| 칸 | 값 | 설명 |
|---|---|---|
| 시장 판단 적중률 | 62% | 주간 자가채점 |
| 개장 방향 예측 | 누적 N일 | `morning_call_daily` 실측 — 20거래일 미만이면 회색 |
| 실전 검증 완료 지표 | 0건 | `FORWARD_VALIDATED` 카운트 |

**하단 주석 (반드시 포함):**

> ★ 개장 방향 예측은 오늘부터 채점을 시작합니다. 20거래일쯤 쌓이면 "이 예측이 몇 % 맞는지"가
> 실제 숫자로 나와요. 그 전까지는 예측을 참고만 하시고, 실제 매매는 「행동 1·2」를 따르시는 게
> 안전합니다.

- **20거래일 미만이면 적중률 %를 표시하지 않는다.** 표본 부족 상태의 백분율은 오해를 부른다.

---

## 4. 디자인 토큰

새벽 창밖의 청회색 지면 + 개장 직전 여명의 호박색 강조. 상태색은 고정이며 테마를 타지 않는다.

```css
--good:#0ca30c;  --warning:#fab219;  --serious:#ec835a;  --critical:#d03b3b;

/* Light */
--paper:#E9EDF1; --panel:#F7F9FB; --panel-2:#DFE5EB; --line:#C3CDD7;
--ink:#111A22;   --ink-soft:#5A6774; --ink-faint:#8B98A5;
--accent:#9A6207; --accent-bg:#F4E6CC; --night:#2C3A4B;

/* Dark */
--paper:#0E1319; --panel:#161D25; --panel-2:#1E262F; --line:#2C3641;
--ink:#EAEEF2;   --ink-soft:#98A5B2; --ink-faint:#6B7A88;
--accent:#E2A748; --accent-bg:#2E2314; --night:#8FA3BA;
```

- 본문 `"Apple SD Gothic Neo","Malgun Gothic"`, 모든 숫자 `font-variant-numeric: tabular-nums`.
- 폭 `max-width: 800px`. 560px 이하에서 시간축 레일을 숨기고 라벨을 본문 위로 올린다.
- 다크모드는 `prefers-color-scheme` + `:root[data-theme]` 양쪽 지원.

---

## 5. 제거 대상

3.0에서 화면에서 **뺀다**(계산·기록은 유지, 표시만 중단):

- 몬테카를로 시뮬레이션 카드
- AI 3사 판단 비교 카드
- 포트폴리오 집중도 카드
- 밸류에이션 상세(역DCF 등) — 보유 카드 안 요약 한 줄로 축소
- 보유 종목 전체 나열 — 바뀐 것만 (§④)
- 종목 무관 거시 뉴스 (§⑥)

---

## 6. 구현 순서

1. **`morning_call.py` 신규** — 판정 규칙 + `morning_call_daily` 적재 + 전일 채점
   (`run_pipeline.py`에서 리포트 생성 **전** 호출, 비치명)
2. **`render_html.py` 골격 교체** — 시간축 레일 + 7블록 구조
3. **①②③ 구현** — 밤사이 / 아침 예측 / 위험 해석
4. **④ 행동1** — 전일 대비 변경 종목 추출
5. **⑤ 행동2** — `_jp_bnf_card` 재작성 (근접 후보·예산 포함)
6. **⑥⑦** — 뉴스 필터 강화, 성적표
7. **`render_md.py`·`render_xlsx.py` 동기화** — 같은 데이터, 같은 순서
8. **`verify_report_quality.py` 갱신** — 아래 수용 기준을 검사에 추가

---

## 7. 수용 기준

구현이 끝났다고 말하기 전에 아래를 전부 확인한다.

- [ ] 밤사이 자료가 없는 날 리포트가 정상 생성되고, ②가 "자료 없음"으로 표시된다
- [ ] 아침 예측이 `morning_call_daily`에 기록되고, 다음 실행에서 전일 행이 채점된다
- [ ] 같은 날 두 번 실행해도 예측 기록이 바뀌지 않는다
- [ ] 위험 블록에 "주범" 문장이 있고, 나라별 3행 모두 "그래서" 한 줄이 있다
- [ ] 행동1의 종목 수 합계 = 실제 보유 종목 수
- [ ] BNF 신호 0건인 날 근접 후보 3개가 나오고, 그것이 매수 지시로 보이지 않는다
- [ ] BNF 커버리지 95% 미만이면 매수 계획이 만들어지지 않고 화면에 경고가 뜬다
- [ ] 예산 사용액이 체결 확정된 포지션만 반영한다
- [ ] 뉴스 0건과 뉴스 수집 실패가 화면에서 구분된다
- [ ] 개장 방향 예측 표본이 20거래일 미만일 때 적중률 %가 표시되지 않는다
- [ ] 검증되지 않은 확률·적중률이 화면 어디에도 없다
- [ ] 라이트/다크 양쪽에서 상태색이 동일하게 읽힌다
- [ ] 360px 폭에서 가로 스크롤이 발생하지 않는다

---

## 8. 열어둔 문제

- **적중률 70% 목표.** 화면 설계로는 달성할 수 없다. `morning_call_daily`가 20거래일
  쌓인 뒤 실측 적중률을 보고, 판정 규칙(§②)의 가중치·재료를 백테스트로 개선하는
  **별도 작업**으로 진행한다. 그 전까지 화면에 70%를 쓰지 않는다.
- **EWY 갭 −20.46의 해석.** 값의 크기가 이례적이다. 산출 방식(전일 종가 대비인지,
  NAV 대비인지)을 `overnight_snapshot` 생성부에서 한 번 확인한 뒤 근거 문장의 표현을
  확정한다. 확인 전까지는 "갭"이라는 중립적 표현을 쓴다.
